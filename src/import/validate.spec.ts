import { validateRows, getCategories } from './validate';



describe('test row validation', () => {
  it('check for correct number of teams', () => {
    const categories = getCategories(rows);
    const rows = [

    ]
  })
})



